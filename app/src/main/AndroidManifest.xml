<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.parttime.sunshine">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name="com.parttime.sunshine.PartTimeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.parttime.sunshine.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 友盟 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="5e098f670cafb27e9200053e" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="${UMENG_CHANNEL}" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name="com.parttime.sunshine.activity.FeedBackActivity" />
        <activity android:name="com.parttime.sunshine.activity.AboutUsActivity" />
        <activity android:name="com.parttime.sunshine.activity.JoinPartTimeActivity" />
        <activity android:name="com.parttime.sunshine.activity.SearchActivity" />
        <activity android:name="com.parttime.sunshine.activity.PersonalResumeActivity" />
        <activity android:name="com.parttime.sunshine.activity.JobDetailActivity" />
        <activity android:name="com.parttime.sunshine.activity.WebActivity" />
        <activity android:name="com.parttime.sunshine.activity.CategoryPartTimeActivity" />
        <activity android:name="com.parttime.sunshine.activity.LoginActivity"></activity>
        <activity android:name="com.parttime.sunshine.activity.MainActivity">

        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>